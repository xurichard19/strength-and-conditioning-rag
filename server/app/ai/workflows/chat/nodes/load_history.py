from app.ai.workflows.chat.state import ChatState

async def history_node(state: ChatState) -> dict:
    """leave the current turn unchanged until conversation history is implemented"""

    return {}
